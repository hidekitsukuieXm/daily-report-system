# ===========================================
# 営業日報システム 環境変数設定
# ===========================================
# このファイルをコピーして .env を作成し、各値を設定してください
# cp .env.example .env

# -------------------------------------------
# アプリケーション設定
# -------------------------------------------
# 環境: development | production | test
NODE_ENV="development"

# サーバー設定
PORT=3000
HOST="localhost"

# -------------------------------------------
# データベース設定
# -------------------------------------------
# PostgreSQL接続文字列
# 形式: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://postgres:password@localhost:5432/daily_report_db"

# -------------------------------------------
# 認証設定 (JWT)
# -------------------------------------------
# JWTシークレットキー (本番環境では必ず強力なランダム文字列を使用)
# 生成例: openssl rand -base64 64
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"

# アクセストークン有効期限 (例: 1h, 30m, 7d)
JWT_EXPIRES_IN="1h"

# リフレッシュトークン有効期限 (例: 7d, 30d)
REFRESH_TOKEN_EXPIRES_IN="7d"

# -------------------------------------------
# CORS設定
# -------------------------------------------
# 許可するオリジン (カンマ区切りで複数指定可能)
CORS_ORIGINS="http://localhost:3000,http://localhost:3001"

# -------------------------------------------
# ファイルアップロード設定
# -------------------------------------------
# アップロードディレクトリ (相対パスまたは絶対パス)
UPLOAD_DIR="./uploads"

# 最大ファイルサイズ (バイト単位) - デフォルト: 10MB
MAX_FILE_SIZE=10485760

# 許可するファイル拡張子 (カンマ区切り)
ALLOWED_FILE_TYPES="pdf,doc,docx,xls,xlsx,ppt,pptx,jpg,jpeg,png"

# -------------------------------------------
# ログ設定
# -------------------------------------------
# ログレベル: debug | info | warn | error
LOG_LEVEL="info"

# -------------------------------------------
# セッション設定 (オプション)
# -------------------------------------------
# セッションシークレット
# SESSION_SECRET="your-session-secret-key"

# セッション有効期限 (秒)
# SESSION_MAX_AGE=86400
